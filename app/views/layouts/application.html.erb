<!DOCTYPE html>
<html>
  <head>
    <title>NewLink</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="./assets/stylesheets/style.css">
  </head>

  <body>
   <div class="container">
    <header class="header">
      <div class="left_right_1">
        <h1 class="grid-6"><a href="/">NewLink</a></h1>
        <% if user_signed_in? %>
          <div class="left_right">
            <%= link_to current_user.name, users_show_path, class: :greeting__link%>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
            <%= link_to "投稿する", new_tweet_path, class: "post" %>
          </div>
        <% else %>
          <div class="left_right">
            <%= link_to "新規登録", new_user_registration_path, class: "post" %>
            <%= link_to "ログイン", new_user_session_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p>
        Reggae, Danchehall, Hiphop, R&B, Aflobeats, Ska, Rocksteday, .. Whole heap a Good Music. NewLink 2023.
      </p>
    </footer>
   </div>
  </body>
</html>
